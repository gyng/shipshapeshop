# Node / web (Vite + React + TS)
node_modules/
dist/
dist-ssr/
*.local
.vite/
coverage/
.eslintcache

# Rust core (→ WASM)
/core/target/
**/target/
*.wasm
pkg/
Cargo.lock

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
*.log

# Env / secrets
.env
.env.*
!.env.example

# Source title art (large raw PNGs) — optimized WebP derivatives ship in web/public/titles
assets/*.png
