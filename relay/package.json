{
  "name": "shape-gacha-relay",
  "private": true,
  "type": "module",
  "description": "Trystero-compatible Nostr signalling relay on a Cloudflare Durable Object (store-nothing, free-tier).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "wrangler deploy --dry-run --outdir dist",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250303.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
